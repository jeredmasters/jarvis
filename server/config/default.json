{
  "port": "env(PORT)",
  "settings": {
    "loggerFormat": "tiny",
    "session": {
      "store": "@foal/typeorm"
    }
  },
  "database": {
    "type": "postgres",
    "host": "env(DB_HOST)",
    "port": "env(DB_PORT)",
    "username": "env(DB_USER)",
    "password": "env(DB_PASS)",
    "database": "env(DB_NAME)"
  }
}